// Created an array with at least 5 strings
let sports = ['basketball', 'soccer', 'tennis', 'baseball', 'hockey'];
console.log('Array of sports:', sports);

// Created a for loop to console log all of the elements plus the index and a comma
for (let i = 0; i < sports.length; i++) {
    console.log((i + 1) + ',' + sports[i]);
}
